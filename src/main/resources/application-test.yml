server:
    port: 8081

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/test
        username: root
        password: root

    mail:
        host: smtp.gmail.com
        port: 587
        username: onlinesupport@eximbay.com  # Gmail 계정
        password: idkduyfkueatzfoo   # Gmail 비밀번호
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
                    connectiontimeout: 5000
                    timeout: 5000
                    writetimeout: 5000

